<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodel="clr-namespace:Common.ViewModel;assembly=Common"
             xmlns:app="clr-namespace:App;assembly=App"
             mc:Ignorable="d"
             x:Class="App.NewItemPage">
    
    <!--<d:ContentPage.BindingContext>

    </d:ContentPage.BindingContext>-->
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel"/>
        <ToolbarItem Text="Save"/>
    </ContentPage.ToolbarItems>

    <StackLayout>
        <ListView 
            SelectionMode="None"
            ItemsSource="{Binding MenuItemEntity.SubItems}"
            VerticalOptions="FillAndExpand"
            HasUnevenRows="True"
            IsPullToRefreshEnabled="True"
            CachingStrategy="RecycleElement">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <!--<EntryCell Label="{Binding Name}" Text="{Binding}" />-->
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Width="120" FontSize="14" Text="Save" Clicked="BtnSaveRegistration_Click"></Button>
    </StackLayout>


</ContentPage>